@page "/productos/nuevo"
@using SPC.Web.Services
@using SPC.Web.Services.Models
@using SPC.Web.Components.Shared
@inject IApiService Api
@inject NavigationManager Navigation
@rendermode InteractiveServer

<PageTitle>Nuevo Product - SPC</PageTitle>

<div class="container-fluid">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/productos">Products</a></li>
            <li class="breadcrumb-item active">Nuevo</li>
        </ol>
    </nav>

    <h4 class="fw-bold mb-4">Nuevo Product</h4>

    @if (errorMessage != null)
    {
        <AlertMessage Message="@errorMessage" 
                      Type="AlertMessage.AlertType.Error" 
                      OnDismiss="() => errorMessage = null" />
    }

    <div class="card">
        <div class="card-body">
            <EditForm Model="producto" OnValidSubmit="HandleSubmit" FormName="CreateProduct">
                <DataAnnotationsValidator />
                
                <div class="row">
                    <div class="col-md-6">
                        <h5 class="mb-3">Datos del Product</h5>
                        
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label for="Code" class="form-label">Código *</label>
                                <InputText id="Code" class="form-control" @bind-Value="producto.Code" />
                                <ValidationMessage For="() => producto.Code" class="text-danger" />
                            </div>
                            <div class="col-md-8 mb-3">
                                <label for="SupplierCode" class="form-label">Código Proveedor</label>
                                <InputText id="SupplierCode" class="form-control" @bind-Value="producto.SupplierCode" />
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="Description" class="form-label">Descripción *</label>
                            <InputText id="Description" class="form-control" @bind-Value="producto.Description" />
                            <ValidationMessage For="() => producto.Description" class="text-danger" />
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="rubro" class="form-label">Category</label>
                                <InputSelect id="rubro" class="form-select" @bind-Value="producto.CategoryId">
                                    <option value="">-- Seleccionar --</option>
                                    @foreach (var rubro in rubros)
                                    {
                                        <option value="@rubro.Id">@rubro.Name</option>
                                    }
                                </InputSelect>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="unidadMedida" class="form-label">Unidad de Medida</label>
                                <InputSelect id="unidadMedida" class="form-select" @bind-Value="producto.UnitOfMeasureId">
                                    <option value="">-- Seleccionar --</option>
                                    @foreach (var um in UnitsOfMeasure)
                                    {
                                        <option value="@um.Id">@um.Name (@um.Code)</option>
                                    }
                                </InputSelect>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <h5 class="mb-3">Precios y Stock</h5>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="CostPrice" class="form-label">Precio Costo</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <InputNumber id="CostPrice" class="form-control" @bind-Value="producto.CostPrice" />
                                </div>
                                <ValidationMessage For="() => producto.CostPrice" class="text-danger" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="SalePrice" class="form-label">Precio Venta</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <InputNumber id="SalePrice" class="form-control" @bind-Value="producto.SalePrice" />
                                </div>
                                <ValidationMessage For="() => producto.SalePrice" class="text-danger" />
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="VATPercent" class="form-label">% IVA</label>
                                <div class="input-group">
                                    <InputNumber id="VATPercent" class="form-control" @bind-Value="producto.VATPercent" />
                                    <span class="input-group-text">%</span>
                                </div>
                                <ValidationMessage For="() => producto.VATPercent" class="text-danger" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="MinimumStock" class="form-label">Stock Mínimo</label>
                                <InputNumber id="MinimumStock" class="form-control" @bind-Value="producto.MinimumStock" />
                                <ValidationMessage For="() => producto.MinimumStock" class="text-danger" />
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="Notes" class="form-label">Notes</label>
                            <InputTextArea id="Notes" class="form-control" rows="3" @bind-Value="producto.Notes" />
                            <ValidationMessage For="() => producto.Notes" class="text-danger" />
                        </div>
                    </div>
                </div>

                <hr />

                <div class="d-flex justify-content-end gap-2">
                    <a href="/productos" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-primary" disabled="@saving">
                        @if (saving)
                        {
                            <span class="spinner-border spinner-border-sm me-1" role="status"></span>
                        }
                        Guardar
                    </button>
                </div>
            </EditForm>
        </div>
    </div>
</div>

@code {
    private CreateProductDto producto = new();
    private List<CategoryDto> rubros = new();
    private List<UnitOfMeasureDto> UnitsOfMeasure = new();
    
    private bool saving = false;
    private string? errorMessage;

    protected override async Task OnInitializedAsync()
    {
        // Load dropdown data in parallel
        var rubrosTask = Api.GetCategorysAsync();
        var unidadesTask = Api.GetUnidadesMedidaAsync();

        await Task.WhenAll(rubrosTask, unidadesTask);

        rubros = await rubrosTask;
        UnitsOfMeasure = await unidadesTask;
    }

    private async Task HandleSubmit()
    {
        saving = true;
        errorMessage = null;

        try
        {
            var result = await Api.CreateProductAsync(producto);

            if (result != null)
            {
                Navigation.NavigateTo("/productos?created=true");
            }
            else
            {
                errorMessage = "Error al crear el producto. Verifique los datos e intente nuevamente.";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error: {ex.Message}";
        }
        finally
        {
            saving = false;
        }
    }
}
