# Review Ledger — reconcile-missing-l2-movements

**Scope:** Accumulated PR2 diff in `reconcile-l2-transaction-operator` worktree.
**Review mode:** Full 4R continuation. `review-resilience` and `review-reliability` were completed before this retry and were intentionally not rerun.
**Retry date:** 2026-08-31

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R1-001 | risk | `scripts/remediation/reconcile-missing-l2-movements/apply.sql:35-78`; `preflight.sql:48-72`; `validate.sql:29-40` | BLOCKER | verified | Source quotes are joined by `Id`, `BranchId`, and `CustomerId`, but not bound to the target movement document number (`q.QuoteNumber = t.DocumentNumber`). `SPC.API/Services/QuoteCommandService.cs:128-134` creates L2 movements using `quote.QuoteNumber`; a wrongly staged quote for the same customer/branch can pass the existing checks and write its amount, resulting in an incorrect L2/current-account balance while preflight and validation report success. Remediated: all qualification/update/validation joins now require quote-document identity; synthetic contracts and the disposable SQL Server `WrongQuoteDocumentIdentity` rollback scenario pass. Scoped risk re-review verified the fix (`preflight.sql:59,71`; `apply.sql:41,64,84`; `validate.sql:25-32`). |
| R1-002 | risk | `scripts/remediation/reconcile-missing-l2-movements/apply.sql:1-106` | CRITICAL | verified | The mutation transaction uses `SET XACT_ABORT ON` and `BEGIN TRANSACTION` but no serializable isolation or locking/protected re-read. A concurrent mutation can alter qualifying data or ledger rows between qualification, update, aggregation, and account-cache update, committing a current-account cache inconsistent with the ledger. This also violates the committed SQL transaction contract in `openspec/changes/reconcile-missing-l2-movements/design.md`. Remediated: the transaction now uses `SERIALIZABLE` isolation with update/hold locks for source requalification and complete-ledger/account-cache derivation; the synthetic contract and disposable SQL Server transaction scenario pass. Scoped risk re-review verified the fix (`apply.sql:5-7,33-41,56-64,99-104,115`). |
| R3-001 | readability | `scripts/remediation/reconcile-missing-l2-movements/{apply.sql,validate.sql}` | WARNING | resolved | Resolution: `apply.sql` and `validate.sql` now declare fixed scope rules as named variables (`@RequiredTargetCount int = 9`, `@RequiredCustomerCount int = 4`, `@RequiredDocumentType int = 20`, `@RequiredInitialBudgetAmount decimal(18,2) = 0`). A future scope amendment has a single declaration point per file. `preflight.sql` retains literals for SELECT-only simplicity; it shares the same scope constants through the PowerShell `Assert-ManifestControls` path. |
| R3-002 | readability | `scripts/remediation/reconcile-missing-l2-movements/{RemediationSafety.ps1,preflight.ps1,validate.ps1}` | WARNING | resolved | Resolution: `RemediationSafety.ps1` now exposes `Invoke-RemediationLauncher`, a shared orchestration helper that owns repository-root resolution, execution-ID generation, protected-path validation, manifest loading/control validation, atomic report writing, and redacted error handling. Both `preflight.ps1` and `validate.ps1` delegate to it with stage-specific parameters and a report-content callback. |
| R3-003 | readability | `scripts/remediation/reconcile-missing-l2-movements/Apply-SyntheticSqlServer.ps1` | SUGGESTION | resolved | Resolution: the unused `$cleanup` script block was removed; all cleanup is in the outer `finally`. |
| R3-004 | readability | `scripts/remediation/reconcile-missing-l2-movements/README.md` | WARNING | resolved | Resolution: README Running-tests section now documents `Apply-SyntheticSqlServer.ps1` as the final suite command with all 23 behavioral scenarios; the description clarifies it is not a token-only check. |
| R4-001 | operational safety | `scripts/remediation/reconcile-missing-l2-movements/README.md` | CRITICAL | resolved-documentation-only | Resolution: README now makes the non-bypassable gate explicit: never run real data without a verified backup and confirmed writer/API exclusion. It explicitly remains PR3 scope. No runner, backup, API control, writer exclusion, real data, or operational execution was implemented or attempted in PR2. |
| R4-002 | reliability | `scripts/remediation/reconcile-missing-l2-movements/{tests/sql-server.tests.ps1,Apply-SyntheticSqlServer.ps1}` | HIGH | resolved | Resolution: the documented SQL Server package test command now invokes the disposable harness, rather than only inspecting its token contract. Each of the 23 behavior scenarios resets to the synthetic fixture baseline; the ambiguity fixture names and drops `PK_Quotes` deterministically. Execution was attempted using Docker-only synthetic resources; this host's Docker Desktop daemon was unavailable, so scenario completion remains environment-blocked. |
| R4-003 | validation correctness | `scripts/remediation/reconcile-missing-l2-movements/validate.sql` | HIGH | resolved | Resolution: read-only validation now rejects both additions and removals of non-target movements and non-scoped accounts relative to the supplied before snapshots. |
| R4-004 | test correctness | `scripts/remediation/reconcile-missing-l2-movements/{Apply-SyntheticSqlServer.ps1,tests/sql-server.tests.ps1}` | HIGH | resolved | Because `Invoke-Scenario` resets the fixture before every action, all preservation and post-apply scenarios must establish their own committed state. `Commit`, `FullLedger`, `PreservedIdentity`, `NonTarget`, `NonScopedAccount`, and `PostChangeValidation` now call shared `Invoke-ValidApply`, which stages the valid 9/4 targets, runs `apply.sql`, and confirms success before assertions. `Invoke-Scenario` is the sole reset authority; negative guards retain baseline-only setup. The static contract enforces both ordering and absence of action-block resets. |
